# Server Configuration
PORT=5000
NODE_ENV=production

# Database Connection (MongoDB Atlas Connection String)
# In production, replace with your MONGODB_URI secret from MongoDB Atlas dashboard.
MONGODB_URI=mongodb+srv://<abhaymaheta228_db_user>:<GfMaqstZydw0RaEC>@cluster0.example.mongodb.net/u1creation?retryWrites=true&w=majority

# CORS Allowed Origin
# Comma-separated list of permitted origins.
# Include both local development (localhost) and production frontend URL (Vercel domain).
CORS_ORIGIN=http://localhost:3000,https://your-first-creation.vercel.app

# Authentication Configuration
JWT_SECRET=your_jwt_secret_key_here
